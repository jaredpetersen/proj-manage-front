'use strict';

angular.module('tangram').controller('MultipleProjectController', function($scope) {
    // create a message to display in our view
    $scope.message = 'This is the project page. Controlled by the MultipleProjectController';
});
