'use strict';

angular.module('tangram').controller('LoginController', function($scope, $stateParams) {
    // create a message to display in our view
    $scope.message = 'This is the login page. Controlled by the ProjectController';
});
